version: '3.8'

services:
  kanban-backend:
    build: ./backend
    ports:
      - "5000:5000"

  
  kanban-frontend:
    build: ./frontend
    ports:
      - "80:80"
    volumes:
      - ./frontend:/usr/share/nginx/html # Adicione esta linha
    depends_on:
      - kanban-backend
